<%= simple_form_for @branding, url: admin_branding_path, html: { method: :put } do |f| %>
  <%= f.input :banner_image, as: :file, label: 'Upload new banner image', required: false %>
  <%= f.button :submit %>
<% end %>
